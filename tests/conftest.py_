import typing as t

import pytest

from izulu import root


@pytest.fixture(scope="session")
def exc():
    class Exc(root.Error):
        MY_CONST: t.ClassVar[int] = 42

        name: str
        age: int

    return Exc
