---
extends: default

ignore:
  - "**/.venv/*"
  - "**/.tox/*"

rules:
  comments:
    require-starting-space: true
    min-spaces-from-content: 1
  quoted-strings:
    quote-type: double
    required: false
    check-keys: true
